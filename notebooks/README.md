# Notebooks

Exploratory notebooks are kept local and are not part of the public release.

The reproducible workflow is script-first. Run the project from the repository root using the commands in `README.md` and `docs/REPRODUCIBILITY.md`.
